import { apiService, setAccessToken, getAccessToken, clearTokens } from './api';

export const authService = {
    async login(email, password) {

    },

    async logout() {

    },

    async refreshToken() {

    },

    getCurrentUser() {

    },
};

export default authService;